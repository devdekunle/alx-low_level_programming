#include "main.h"
#include "2-strlen.c"
/**
*reverse_array - program to revers array
*@a: array to reverse
*@n: length of array
*Return: nothing
*/

void reverse_array(int *a, int n)
{
	int *temp[];

	for (i = 0; *(a + i) < n; i++)
	{
		*(temp + n) = *(a + i);
		n--;

	}


}
