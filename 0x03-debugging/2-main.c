#include "main.h"
#include <stdio.h>

/**
* main - prints the largest of three numbers
* Return: Always 0
*/

int main()
{
	int a, b, c;
	int largest;

	a = 972;
	b =  -98;
	c = 0;

	largest = largest_number(a, b, c);

	return (largest);

}

